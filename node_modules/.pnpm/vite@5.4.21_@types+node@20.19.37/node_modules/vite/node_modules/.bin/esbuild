#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

exec "$basedir/../../../esbuild/bin/esbuild"   "$@"
exit $?
# cmd-shim-target=/home/runner/work/booked-ranked-fundable/booked-ranked-fundable/node_modules/.pnpm/vite@5.4.21_@types+node@20.19.37/node_modules/esbuild/bin/esbuild
